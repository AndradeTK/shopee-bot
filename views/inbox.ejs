<%- include('partials/header', { title: 'Caixa de Entrada' }) %>

<div class="card">
    <div class="card-header">
        <i class="fa-solid fa-inbox"></i> Caixa de Entrada de Mensagens
    </div>
    <div class="card-body">
        <p class="text-muted">Aqui são exibidas as últimas 50 mensagens recebidas por qualquer grupo. Use esta tela para encontrar facilmente o ID de um grupo para configurar.</p>
        <div class="table-responsive">
            <table class="table table-striped table-hover">
                <thead>
                    <tr>
                        <th>Data/Hora</th>
                        <th>Nome do Grupo</th>
                        <th>ID do Grupo (Clique para copiar)</th>
                        <th>Remetente</th>
                        <th>Mensagem</th>
                    </tr>
                </thead>
                <tbody>
                    <% messages.forEach(msg => { %>
                        <tr>
                            <td style="white-space: nowrap;"><%= new Date(msg.timestamp).toLocaleString('pt-BR') %></td>
                            <td><%= msg.groupName %></td>
                            <td>
                                <span class="badge bg-secondary text-monospace" style="cursor: pointer;" onclick="copyToClipboard('<%= msg.groupId %>')">
                                    <%= msg.groupId %> <i class="fa-regular fa-copy"></i>
                                </span>
                            </td>
                            <td><%= msg.senderName %></td>
                            <td><small><%= msg.messageText.substring(0, 100) %>...</small></td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        </div>
    </div>
</div>

<%- include('partials/footer') %>